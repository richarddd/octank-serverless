const routes = {
  documents: "/documents",
  documentsNew: "/documents/new",
  documentsEdit: "/documents/:id",
  index: "/",
  admin: "/admin",
  auth: "/auth/*",
  support: "/support",
  logout: "/logout",
};

export default routes;
